# ChatUI v1.7.18

Released on July 22, 2026.

## Fixes

- Kept fenced code blocks visually consistent while an answer is still streaming: once a closing fence arrives, the rendered block retains the same rounded code container instead of briefly switching to a square `pre` element.
- Preserved deferred copy controls and other final Markdown enhancements so streaming stays lightweight.

## Verification

- `npm run check` passes before release.
